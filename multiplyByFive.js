/* Create a function `multiplyByFive` that multiply's a number by 5. Make sure to return the value.

The Function takes a number parameter and returns the value of the number multiplied by 5.

Example:

INPUT: multiplyByFive(4);
OUTPUT: 20 
*/

function multiplyByFive(num){
	return num * 5;
    
}

multiplyByFive(5);