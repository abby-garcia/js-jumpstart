/*
Create the function `maxOfThree` that evaluates 
three numbers and returns the largest number out of the three.

The function takes three number 
arguments and returns the highest number out of three.

**Examples*

- INPUT: `maxOfThree(5,4,10);`
- OUTPUT: `10`

- INPUT: `maxOfThree(7,7,4);`
- OUTPUT: `7`
*/

function maxOfThree(num1, num2, num3){

	return Math.max(num1, num2, num3);

    
}

maxOfThree(89, 5000, 1);




// Mat.max is a function, so all you have to do is put in the thing you need to figure out
// inside of that function. 