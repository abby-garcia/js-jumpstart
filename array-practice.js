// **STEP** Create an Empty Array and set it to the variable myArray

var empty = [];



// **STEP** Create a new Array with the values 160 and "Pearl Street" , set it to the variable address

var address = [160, "PearlStreet"];


// **STEP** Push (insert) the value "Hello" into myArray's Array. After use console.log to see what is in the array.
// ex console.log(myArray);

address.push("Hello");
console.log(address);



// **STEP** Call the length property on myArray, it should return 1 (since you added a value to it in the last step).

address.length;


// **STEP** Pop (remove) the value out of myArray.  After use console.log to see what is in the array.
// ex console.log(myArray);

address.pop();
console.log(address);

// **STEP** Create a new Array with the values "z", "b", "d", "g" and set it to the variable multipleValues

var multipleValues = ["z", "b", "d", "g"]

// **STEP** multipleValues is not sorted.  Sort the array so it is in alphabetical order (hint: use the sort method)

multipleValues.sort();

// **STEP** check if the multipleValues array holds the value of "a".  Use the .indexOf method, the return result should
// be -1

address.indexOf("a");





